[project]
name = "agent-sprite-forge"
version = "0.1.0"
description = "Agent skills and deterministic local processors for 2D game sprites and maps"
requires-python = ">=3.10"
dependencies = [
    "numpy>=1.26,<3",
    "Pillow>=10,<13",
]

[tool.uv]
package = false
